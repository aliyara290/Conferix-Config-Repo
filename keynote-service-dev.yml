spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/keynote_db
    username: aliyara29
    password: Yara2001
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:8090/realms/Conferix"